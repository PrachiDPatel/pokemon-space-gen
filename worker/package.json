{
  "name": "pokepod-proxy",
  "private": true,
  "scripts": {
    "login": "wrangler login",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put NASA_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
